const express = require('express')
const router = express.Router()

// const {getActivities, deleteActivity, markAsRead, getUnreadCount} = require("../controllers/activityController")

// const requireAuth = require("../middleware/requireAuth")
// router.use(requireAuth)


// router.get("/getActivities/:userid",getActivities)

// router.delete("/deleteActivity",deleteActivity)
// router.patch("/markAsRead", markAsRead)
// router.get("/getUnreadCount/:userid", getUnreadCount);

module.exports = router